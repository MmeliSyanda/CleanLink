# Copy this file to .env and fill in values before running locally.
# Both variables are required only for `pnpm run dev`.
# For `pnpm run build` (and Netlify) they default to PORT=3000 and BASE_PATH=/.

PORT=3000
BASE_PATH=/
